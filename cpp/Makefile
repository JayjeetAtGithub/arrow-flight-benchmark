server:
    rm server
	g++ -std=c++11 server.cc -larrow -larrow_dataset -larrow_flight -lparquet -o server

client:
	rm client
	g++ -std=c++11 client.cc -larrow -larrow_dataset -larrow_flight -lparquet -o client
